import * as React from 'react';

export default function WelcomePage() {
  return (
    <main style={{padding: 24}}>
      <h1>Welcome (Pages Router)</h1>
      <p>Use this folder for legacy or new pages you want to serve via the Pages Router.</p>
    </main>
  );
}
